import React from 'react';
import { Typography, Box } from '@mui/material';

const AgentDashboard = () => {
  return (
    <Box>
      <Typography variant="h4">Agent Chat Dashboard</Typography>
      <Typography>Chat interface will be here.</Typography>
    </Box>
  );
};

export default AgentDashboard;
